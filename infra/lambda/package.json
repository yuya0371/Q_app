{
  "name": "q-lambda-functions",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "esbuild --bundle --platform=node --target=node20 --outdir=dist --external:@aws-sdk/*"
  },
  "dependencies": {
    "@aws-sdk/client-cognito-identity-provider": "^3.0.0",
    "@aws-sdk/client-dynamodb": "^3.0.0",
    "@aws-sdk/client-s3": "^3.0.0",
    "@aws-sdk/lib-dynamodb": "^3.0.0",
    "@aws-sdk/s3-request-presigner": "^3.0.0",
    "uuid": "^9.0.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.145",
    "@types/node": "^20.0.0",
    "@types/uuid": "^9.0.0",
    "esbuild": "^0.24.0",
    "typescript": "^5.0.0"
  }
}
